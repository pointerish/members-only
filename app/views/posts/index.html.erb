<div class="main-content">
  <h1>See all Posts</h1>
    <% @posts.each do |post| %>
        <div class="post-card">
          <div class="post-title">
            <%= link_to(post.title, "posts/show?id=#{post.id}") %>
          </div>
          <div class="posted-by">
            <%= posted_by(post) %>
          </div>
        </div>
    <% end %>
</div>